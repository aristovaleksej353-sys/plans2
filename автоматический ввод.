import sqlite3

connection = sqlite3.connect('travel.db')
cursor = connection.cursor()

cursor.execute("PRAGMA foreign_keys = ON")

# Удаление старых таблиц
cursor.execute("DROP TABLE IF EXISTS travel_plans")
cursor.execute("DROP TABLE IF EXISTS countries")
cursor.execute("DROP TABLE IF EXISTS users")

# Создание таблиц
cursor.execute("""CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    age INTEGER NOT NULL
    )""")

cursor.execute("""CREATE TABLE countries (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    users_id INTEGER NOT NULL,
    name TEXT NOT NULL,
    best_season TEXT NOT NULL,
    has_sea INTEGER NOT NULL,
    FOREIGN KEY (users_id) REFERENCES users(id)
    )""")

cursor.execute("""CREATE TABLE travel_plans (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    users_id INTEGER NOT NULL,
    country_id INTEGER NOT NULL,
    planned_year INTEGER NOT NULL,
    days_count INTEGER NOT NULL,
    date_returned TEXT,
    FOREIGN KEY (users_id) REFERENCES users(id),
    FOREIGN KEY (country_id) REFERENCES countries(id)
    )""")

# СЛУЧАЙНЫЕ ДАННЫЕ 

# Пользователи
users_array = [("Петя", 17), ("Илья", 18), ("Надя", 19), ("Марина", 20), ("Анна", 25)]
cursor.execule(""" skl: 
INSERT INTO users (name, age) VALUES (?, ?)
+""", users_array)

# Страны 
countries_array = [
    (1, "Италия", "май-сентябрь", 1),
    (1, "Франция", "апрель-июнь", 1),
    (2, "Япония", "март-май", 1),
    (2, "Таиланд", "ноябрь-февраль", 1),
    (3, "Турция", "май-октябрь", 1),
    (4, "Испания", "май-октябрь", 1),
    (5, "Греция", "июнь-сентябрь", 1)
]
cursor.execule(""" skl: 
INSERT INTO countries (users_id, name, best_season, has_sea) VALUES (?, ?, ?, ?)
+""", users_array)

#Планы поездок
plans_array = [
    (1, 1, 2025, 7, "2025-06-15"),
    (1, 2, 2026, 5, ),
    (2, 3, 2025, 10, "2025-04-20"),
    (2, 4, 2026, 8, ),
    (3, 5, 2025, 6, "2025-08-10"),
    (4, 6, 2026, 7, ),
    (5, 7, 2025, 4, "2025-07-01")
]

cursor.execule(""" skl: 
INSERT INTO countries (users_id, country_id, planned_year, days_count, date_returned) VALUES (?, ?, ?, ?, ?)"
+""", users_array)
